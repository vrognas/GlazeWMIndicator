name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build and bundle
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: ./scripts/bundle.sh release

      - name: Import signing certificate
        env:
          CERTIFICATE_P12: ${{ secrets.CERTIFICATE_P12 }}
          CERTIFICATE_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/signing.keychain-db"
          CERT_PATH="$RUNNER_TEMP/certificate.p12"

          echo "$CERTIFICATE_P12" | base64 --decode > "$CERT_PATH"

          security create-keychain -p "" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"

          security import "$CERT_PATH" -k "$KEYCHAIN_PATH" -P "$CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH" login.keychain-db

      - name: Sign app
        run: |
          codesign --sign "Developer ID Application: STEIN VIKTOR ROGNAS (6F9TBQ4LVA)" \
            --force \
            --options runtime \
            --timestamp \
            ".build/GlazeWM Indicator.app"

      - name: Create zip
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          cd .build
          zip -r "glazewm-indicator-${VERSION}.zip" "GlazeWM Indicator.app"

      # TODO: Re-enable notarization when Apple's service is responsive
      # - name: Notarize
      # - name: Staple
      # - name: Re-zip after stapling

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ github.ref_name }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release create "$TAG_NAME" \
            ".build/glazewm-indicator-${VERSION}.zip" \
            --title "$TAG_NAME" \
            --generate-notes
